apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pfsense-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "pfsense-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pfsense-exporter.fullname" . }}:{{ .Values.config.port }}']
  restartPolicy: Never
